# Go Client Fixtures

Planned contents:

- per-case Go client binaries
- assertion helpers
- result marshaling back to host runner

Current baseline asset:

- `baseline/main.go`

This binary exercises `IBaselineService` against a service-manager-registered Java fixture service.
